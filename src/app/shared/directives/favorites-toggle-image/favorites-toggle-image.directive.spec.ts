import { FavoritesToggleImageDirective } from './favorites-toggle-image.directive';

describe('FavoritesToggleImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoritesToggleImageDirective();
    expect(directive).toBeTruthy();
  });
});
